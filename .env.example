# Nuxt app (public — safe to expose in the client bundle)
NUXT_PUBLIC_CMS_BASE=http://localhost:8000
NUXT_PUBLIC_CMS_SITE=deve-site

# scripts/create-cms-forms.mjs only — the token is SECRET, never NUXT_PUBLIC_*
CMS_BASE=http://localhost:8000
CMS_SITE=deve-site
CMS_API_TOKEN=
